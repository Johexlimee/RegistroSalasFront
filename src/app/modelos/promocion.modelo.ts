export class Promocion {
    id_prom: number; // ID de la promoción
    nombre: string; // Nombre de la promoción
  
    constructor(id_prom: number, nombre: string) {
      this.id_prom = id_prom;
      this.nombre = nombre;
    }
  }
  